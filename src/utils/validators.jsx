import validator from 'validator';

class Validators {
   // isEmail(email){
   //    let regexStr = /^[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/igm;

   //    let response = {
   //       valid   : validator.isEmail(email),
   //       clearTxt: email.replace(regexStr,'')
   //    };
   //    return response;
   // }

   onlyText(txt){

   }
}

export default(new Validators());

